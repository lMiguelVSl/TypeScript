"use strict";
(() => {
    let isSuperman = true;
    let isBatman = false;
    isBatman = (isBatman) ? true : false;
    console.log(isBatman); //show the value
    console.log({ isBatman }); //show the obj and the value 
})();
