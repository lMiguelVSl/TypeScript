"use strict";
(() => {
    const numbers = [1, 2, 3, 4, 'miguel', true, 7, 8];
    console.log(numbers);
    const number = [1];
    console.log(number);
    const villains = ['voldemort', 'Miguelinho', 'Loki'];
    villains.forEach(v => console.log(v.toUpperCase()));
})();
